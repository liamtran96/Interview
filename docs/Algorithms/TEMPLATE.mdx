---
sidebar_position: 999
---

import AlgorithmProblem from '@site/src/components/AlgorithmProblem';

# 🎯 Template - Copy This to Create New Problems

<AlgorithmProblem
  title="Problem Name"
  difficulty="Easy"
  description={`
<p>Problem description goes here.</p>
<p>You can use <code>inline code</code> and <strong>bold text</strong>.</p>
<h4>Constraints:</h4>
<ul>
  <li>Constraint 1</li>
  <li>Constraint 2</li>
</ul>
`}
  examples={[
    {
      input: 'Input example',
      output: 'Output example',
      explanation: 'Why this works (optional)'
    }
  ]}
  starterCode={`function exampleFunction(nums, target) {
  // TODO: Write your solution here
  // Replace this function name and parameters with your problem
  return 0;
}`}
  solution={`function exampleFunction(nums, target) {
  // Replace with your optimal solution
  // Example: return nums[0] + nums[1];
  return 0;
}`}
  testCases={[
    {
      // IMPORTANT: Replace these with your actual test values
      // For single param: input: [value]
      // For multiple params: input: [param1, param2, param3]
      input: [[1, 2, 3], 5],
      expected: 3,
      description: 'Test case description - explain what this tests'
    },
    {
      input: [[4, 5, 6], 10],
      expected: 6,
      description: 'Edge case description'
    }
  ]}
  functionName="exampleFunction"
/>

## Solution Explanation

### Approach 1: Brute Force

**Time:** O(?)
**Space:** O(?)

```javascript
// Brute force code
```

### Approach 2: Optimal ✅

**Time:** O(?)
**Space:** O(?)

```javascript
// Optimal solution
```

**How it works:**
1. Step 1
2. Step 2
3. Step 3

## Common Mistakes

❌ Mistake 1
✅ Correct way

## Interview Tips

- Tip 1
- Tip 2
- Tip 3
